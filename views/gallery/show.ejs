<% include ../partials/header %>

<h1>Show page for Gallery</h1>

<div>
    <h2><%= post.title %></h2>
    <img src="<%= post.image %>" alt="">
    <p>
        <strong><%= post.description %></strong>
    </p>
    <p><%- post.body %></p>
    
    <a href="/">
        <button>Go Home</button>
    </a>
</div>

<% include ../partials/footer %>